# 작업 일지 - 2026-05-23

## 작업 목표
- [x] `/projects` 페이지 최상단(수상내역 위)에 "5기 프로젝트 일정" 섹션 추가
- [x] 일정 섹션 내부에 "5기 프로젝트 참여 신청" 구글폼 CTA 버튼 1개 추가

## 질문 및 답변
### Q1: 일정 섹션 내용을 어떻게 관리할까? (정적 vs 관리자 연동)
**답변**: 코드에 직접 작성 (정적)
**결정**: 일정 내용을 컴포넌트 내 상수(`PHASES`, `SPECIAL_EVENT`)로 하드코딩

### Q2: 참여 신청 CTA 버튼 링크는?
**답변**: 구글폼으로 연결. 상단 header 메뉴의 "5기 프로젝트 참여 신청" 버튼 참고
**결정**: 헤더 CTA와 동일하게 `siteConfig.primaryCtaLink`(수동 모드 구글폼 링크)를 사용. URL을 코드에 박지 않고 DB 단일 소스 유지

### Q3: CTA 버튼 배치는?
**답변**: 일정 섹션 안에 1개
**결정**: 일정 섹션 하단 중앙에 버튼 1개 배치

## 작업 내역
### 신규 컴포넌트 - ProjectScheduleSection
- 변경 파일: `src/app/(site)/projects/components/ProjectScheduleSection.tsx` (신규)
- 내용:
  - 단계 카드 2개: "프로젝트 등록 및 홍보 (5주)", "프로젝트 운영 (6주)" — 각 마일스톤(라벨/날짜/비고) 타임라인 표시
  - 특별 행사 배너: "SW 경진대회 출전 행사 (26-08-29 ~ 30)"
  - CTA: `applyLink`가 외부(http)면 `<a target="_blank">`, 내부면 `<Link>`로 분기. `applyLink` 없으면 버튼 숨김
  - primary 컬러 기반 디자인, 서버 컴포넌트(상호작용 없음)

### 페이지 연동
- 변경 파일: `src/app/(site)/projects/page.tsx`
- 내용:
  - `siteConfigRepository.getSiteConfig()`를 `Promise.all`에 추가
  - `applyLink = siteConfig?.primaryCtaLink ?? ""` 도출
  - `<ProjectScheduleSection applyLink={applyLink} />`를 `<AwardsSection>` 위(최상단)에 렌더링

## 검증
- `npx tsc --noEmit` → exit 0 (타입 에러 없음)

## 비고 / 다음 작업
- 5기 종료 또는 6기 전환 시 일정 내용은 코드 수정 필요(정적 하드코딩). 운영자가 직접 바꿔야 한다면 추후 admin 연동으로 확장 가능.
- 헤더 CTA가 auto 모드로 바뀌면 `primaryCtaLink`가 비어 일정 섹션 버튼이 숨겨질 수 있음(현재는 manual 모드 + 구글폼).
- 브라우저 시각 확인(반응형/렌더링)은 미수행 — dev 서버로 확인 필요.
