# 작업 일지 - 2026-05-19

## 작업 목표
- [x] SiteConfig 엔티티에 CTA 필드 추가
- [x] 어드민 메뉴에 CTA 항목 추가
- [x] CTA 관리 어드민 페이지 생성
- [x] Header.tsx에 ctaMode 분기 처리 추가
- [x] HeroSection.tsx에 CTA props 추가
- [x] 홈페이지에 CTA 설정 전달
- [x] 빌드 검증

## 현재 상태 분석
### 기존 구조
- **SiteConfig**: `navCtaText`, `navCtaLink` 필드가 있지만 사용되지 않음
- **Header.tsx**: `isRecruitmentOpen` 상태에 따라 CTA 자동 생성
- **HeroSection.tsx**: 동일한 로직 + "더 알아보기" 버튼 고정
- **HeroWrapper.tsx**: 서버 컴포넌트에서 SiteConfig 조회 후 HeroSection에 전달

### 설계 결정
- `ctaMode: "auto" | "manual"` 필드 추가
- `auto`: 기존처럼 모집 상태 기반 자동 생성
- `manual`: 어드민에서 설정한 텍스트/링크 사용
- 기존 `navCtaText`, `navCtaLink`는 하위 호환성을 위해 유지

## 작업 내역

### Phase 1: SiteConfig 엔티티 확장
- 변경 파일: `src/domain/entities/index.ts`
- 추가 타입: `CtaMode = "auto" | "manual"`
- 추가 필드: `ctaMode`, `primaryCtaText`, `primaryCtaLink`, `secondaryCtaText`, `secondaryCtaLink`
- 기존 필드 `navCtaText`, `navCtaLink`에 @deprecated 주석 추가

### Phase 2: 어드민 메뉴 설정
- 변경 파일: `src/shared/constants/admin.ts`
- `MousePointerClick` 아이콘으로 CTA 버튼 설정 메뉴 추가
- `/admin/cta` 경로 및 페이지 제목 등록

### Phase 3: CTA 관리 어드민 페이지
- 생성 파일: `src/app/admin/cta/page.tsx`
- RadioGroup을 이용한 자동/수동 모드 전환 UI
- 수동 모드에서 메인/보조 버튼 텍스트/링크 설정 가능
- 미리보기 Dialog로 설정 결과 확인 가능

### Phase 4: Header.tsx 수정
- 변경 파일: `src/presentation/components/layout/Header.tsx`
- `ctaMode === "manual"` 분기 추가
- 수동 모드 시 `primaryCtaText`, `primaryCtaLink` 사용

### Phase 5: HeroSection.tsx 수정
- 변경 파일: `src/presentation/components/sections/HeroSection.tsx`
- `CtaConfig` 인터페이스 추가
- `getPrimaryCta()`, `getSecondaryCta()` 헬퍼 함수로 CTA 결정
- 조건부 렌더링 제거하고 동적 값 사용

### Phase 6: HeroWrapper.tsx 수정
- 변경 파일: `src/presentation/components/sections/HeroWrapper.tsx`
- SiteConfig에서 CTA 설정 추출하여 HeroSection에 전달

## 발생 이슈

### 이슈 1: RadioGroup 컴포넌트 누락
- 원인: shadcn/ui의 RadioGroup이 프로젝트에 설치되어 있지 않음
- 해결: `npx shadcn@latest add radio-group -y`로 설치

## 변경된 파일 목록
- `src/domain/entities/index.ts` - SiteConfig에 CTA 필드 추가
- `src/shared/constants/admin.ts` - CTA 메뉴 추가
- `src/app/admin/cta/page.tsx` - 신규 생성
- `src/presentation/components/layout/Header.tsx` - ctaMode 분기
- `src/presentation/components/sections/HeroSection.tsx` - CTA props
- `src/presentation/components/sections/HeroWrapper.tsx` - CTA 설정 전달
- `src/components/ui/radio-group.tsx` - shadcn/ui 컴포넌트 추가

## 검증 결과
- ✅ `yarn build` 성공 (56.24s)
- ✅ 모든 페이지 정적/동적 생성 완료

## 다음 작업
- 실제 브라우저에서 /admin/cta 테스트
- 수동 모드 설정 후 홈페이지 및 네비게이션 반영 확인

---

## 추가 작업: 푸터 소셜 아이콘 추가

### 요구사항
- 푸터에 인스타그램, 티스토리, 카카오톡 아이콘 추가
- 아이콘은 검은색으로 통일
- 소셜 링크는 사이트 정보 설정(/admin/settings)에서 관리

### 변경 파일
- `src/domain/entities/index.ts` - `instagramLink`, `blogLink` 필드 추가
- `src/app/admin/settings/page.tsx` - 소셜 채널 카드 추가
- `src/presentation/components/common/SocialIcons.tsx` - **신규** - 커스텀 소셜 아이콘 컴포넌트
- `src/presentation/components/common/index.ts` - SocialIcons export 추가
- `src/presentation/components/layout/Footer.tsx` - 소셜 아이콘 섹션 추가

### 구현 내용
1. **SiteConfig 확장**: `instagramLink`, `blogLink` 필드 추가 (chatLink는 기존 필드 활용)
2. **어드민 설정 페이지**: 소셜 채널 카드에 인스타그램, 블로그, 카카오톡 채널 링크 입력 필드 추가
3. **커스텀 아이콘**: InstagramIcon, TistoryIcon, KakaoTalkIcon SVG 컴포넌트 생성
4. **푸터 수정**: 로고 아래에 소셜 아이콘 추가, 링크가 설정된 경우에만 표시

### 검증 결과
- ✅ `yarn build` 성공 (49.91s)
