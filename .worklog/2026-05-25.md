# 작업 일지 - 2026-05-25

## 작업 목표
- [x] `/Users/cheetah_hong/Downloads/workout_dashboard_1.html`(운동 챌린지 대시보드)를 `/club/urbanfit` 엔드포인트로 추가
- [x] 원본 HTML 구조/내용은 유지하되 그로스로그 홈페이지 테마(폰트·컴포넌트·컬러)에 맞춰 재구성

## 주요 결정 사항
### 라우트 위치
- **결정**: `src/app/(site)/club/urbanfit/page.tsx`
- **이유**: `(site)` 라우트 그룹 아래 두면 공통 Header/Footer/카카오톡 버튼과 홈페이지 테마가 자동 적용되어 "홈페이지 스타일로 테마 맞춰서" 요구를 충족

### 테마 매핑 (원본 HTML → 사이트 디자인 토큰)
- accent `--green #00C26E` → 브랜드 `green-1 #00962B` (`text-green-1` / `bg-green-1`)
- soft green 배경 `--green-soft` → `bg-green-1/10`
- ink/foreground `#0a0a0a` → `text-foreground` / `bg-foreground` (`#222222`)
- ink-soft/ink-mute → `gray-1`/`gray-2`/`gray-3`
- surface/line → `gray-7`/`bg-card`/`border-border`
- JetBrains Mono → 사이트 모노폰트 `font-mono`(Geist Mono)
- Pretendard → 사이트 기본 sans 그대로
- featured/gold 카드(검정 배경) 위 그린 강조는 가독성 위해 `green-5`

## 변경/생성 파일
- `src/app/(site)/club/urbanfit/data.ts` — 멤버·주차·KPI·명예의전당 데이터 + 타입
- `src/app/(site)/club/urbanfit/components/MemberTable.tsx` — 멤버별 주차 인증 테이블(성공/실패/미참여 셀 컬러링)
- `src/app/(site)/club/urbanfit/components/UrbanfitDashboard.tsx` — 헤더/KPI/주차별/명예의전당/푸터 조합
- `src/app/(site)/club/urbanfit/page.tsx` — 라우트 + 메타데이터

## 검증
- dev 서버에서 `/club/urbanfit` 렌더 확인: 멤버 14행, 주차 10주, KPI 4종, 명예의전당 3종, 푸터 정상
- computed style 확인: 강조 그린 `rgb(0,150,43)`, 셀 그린 10% 배경, TOTAL 헤더 `#222222`/흰색, 본문 Pretendard, 라벨 Geist Mono
- 데스크톱(4열)·모바일(2열) 반응형 정상, 콘솔 에러 없음

---

## 추가 작업 - 폴리글랏(외국어 학습) 클럽 + 공용 컴포넌트 추출

### 결정: 대시보드 공용화
- urbanfit/polyglot 구조가 동일 → 데이터 주도형 공용 컴포넌트로 추출
- `club/_shared/{types.ts, MemberTable.tsx, ChallengeDashboard.tsx}` 생성
- 각 클럽은 `data.ts`(ChallengeData 객체) + 얇은 `page.tsx`만 보유 → 향후 클럽 추가 용이
- urbanfit 리팩터링: data.ts를 `ChallengeData` 객체로 전환, 기존 `urbanfit/components/*` 삭제

### 폴리글랏 데이터 집계 (사용자 제공 주차별 결산 파싱)
- 멤버 8명(이준혁 8주차 합류, W1~7 미참여), 총 193회 인증
- 검증: 각 주차 멤버 합 = 주차 total, 멤버 total 합 = 193 일치
- KPI: 193회 / 8명 / 19.3회 / 개근 1명(남혜민)
- Honor: MVP 안혜린 32회, 개근상 남혜민 30회, 꾸준상 박창현·부혜선 28회

### 생성/변경 파일
- `club/_shared/types.ts`, `club/_shared/MemberTable.tsx`, `club/_shared/ChallengeDashboard.tsx`
- `club/urbanfit/data.ts`(객체화), `club/urbanfit/page.tsx`(공용 컴포넌트 사용), `club/urbanfit/components/*` 삭제
- `club/polyglot/data.ts`, `club/polyglot/page.tsx`

### 검증
- 깨끗한 재시작 후 `/club/polyglot`·`/club/urbanfit` 모두 HTTP 200, 빌드 에러 0
- 폴리글랏 DOM 확인: 8행, 안혜린 32회(7/10), 이준혁 W8~ 9회(2/10), 푸터 "총 193회 인증 완료 📚"
- (참고) 리팩터링 중간 단계에서 삭제된 구 컴포넌트 참조로 stale Turbopack 에러가 콘솔에 남았으나, dev 서버 재시작으로 해소

---

## 추가 작업 (2026-05-26) - 랭킹 기준 변경: 꾸준함(성공/10) 우선

### 요구사항
- 멤버 테이블 정렬을 total 내림차순 → **성공 주차(성공/10) 내림차순, 동률 시 total 내림차순**으로 변경 (polyglot·urbanfit 둘 다)
- 명예의 전당 MVP 우선순위도 동일 기준 → polyglot MVP는 남혜민(10주 개근)

### 변경
- `_shared/MemberTable.tsx`: 렌더 전 `[...members].sort((a,b)=>b.success-a.success || b.total-a.total)` (비파괴 정렬) → 두 클럽 공통 적용
- `_shared/ChallengeDashboard.tsx`: 명예의 전당 stat 구분자 `/` → `·`
- `polyglot/data.ts` honors: MVP 남혜민(10/10·총30회), 박창현(9/10·총28회), 부혜선(8/10·총28회)
- `urbanfit/data.ts` honors: MVP 부혜선(10/10·총33회), 남혜민(10/10·총30회), 이준혁(8/10·총29회) — 3위 박예승(7주)→이준혁(8주)로 교체
- honor `big`을 "성공/10" 비율로 표기(테이블 헤더와 동일 지표), 총 횟수는 detail로

### 검증
- 두 라우트 HTTP 200, 테이블/명예의전당 순서 DOM 확인
  - polyglot: 남혜민→박창현→부혜선→안혜린→이예솔→김태홍→김민경→이준혁
  - urbanfit: 부혜선→남혜민→이준혁→김태홍→박예승→… (동률 total 정렬 정상)
- 전체 페이지 스크린샷 캡처: `~/Downloads/polyglot_v2.png`, `~/Downloads/urbanfit_v2.png`

---

## 추가 작업 (2026-05-26) - 명예의 전당 카드 구성 조정

### 요구사항
- urbanfit 꾸준상: 성공 8/10 이상자 중 상위 카드(부혜선·남혜민) 제외하고 모두 표기
- polyglot 2번째 카드: 꾸준상 대신 "최다상"(total 최다) → 안혜린
- polyglot 3번째 꾸준상: urbanfit처럼 8/10 이상자(MVP 제외) 표기

### 최종 명예의 전당
- urbanfit: ①MVP 완벽개근 부혜선(10/10·총33) ②RUNNER-UP 개근 남혜민(10/10·총30) ③꾸준상 8주이상 이준혁·김태홍(8/10·각8주)
- polyglot: ①MVP 완벽개근 남혜민(10/10·총30) ②최다상 최다인증 안혜린(32회·7주성공) ③꾸준상 8주이상 박창현·부혜선(8/10↑·9·8주)
- 변경 파일: `polyglot/data.ts`, `urbanfit/data.ts` (honors만)
- 검증: 두 라우트 HTTP 200, honor rank/name DOM 확인, 스크린샷 `~/Downloads/{polyglot,urbanfit}_v3.png`
